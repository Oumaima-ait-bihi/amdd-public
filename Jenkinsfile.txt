pipeline {
    agent any

    tools {
        sonarQubeScanner 'sonar-scanner'
        jdk 'Java17' // adapte selon ton projet
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                sh './mvnw clean install' // ou ton build Gradle
            }
        }

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('SonarQube') {
                    sh './mvnw sonar:sonar -Dsonar.projectKey=amdd-app -Dsonar.host.url=http://localhost:9000 -Dsonar.login=squ_20254bb88475b7acf8e15383f0d7d8a5d9c73bfb'
                }
            }
        }
    }
}
